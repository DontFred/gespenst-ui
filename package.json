{
  "name": "gespenst",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "rm -rf node_modules && rm bun.lockb"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.3",
    "@gespenst/typescript-config": "*",
    "eslint-config-gi": "^0.1.3",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.1.8",
  "prettier": "eslint-config-gi/prettier",
  "workspaces": [
    "apps/*",
    "packages/*",
    "gespenst/core/*",
    "gespenst/components/*"
  ]
}
